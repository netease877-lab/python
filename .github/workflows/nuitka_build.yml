name: Compile N6000 OpenVINO Wheels

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-wheels:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo apt-get clean

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential git python3-venv python3-dev patchelf ccache ocl-icd-opencl-dev

      - name: Setup ccache
        uses: actions/cache/restore@v4
        with:
          path: ~/.cache/ccache
          key: ccache-openvino-n6000-${{ github.run_id }}
          restore-keys: |
            ccache-openvino-n6000-
            ccache-openvino-

      - name: Configure ccache environment (VERY IMPORTANT)
        run: |
          echo "CCACHE_DIR=${HOME}/.cache/ccache" >> $GITHUB_ENV
          echo "CCACHE_BASEDIR=$GITHUB_WORKSPACE" >> $GITHUB_ENV
          echo "CCACHE_NOHASHDIR=1" >> $GITHUB_ENV
          echo "CCACHE_SLOPPINESS=pch_defines,time_macros,file_macro,include_file_mtime" >> $GITHUB_ENV
          echo "CCACHE_COMPRESS=true" >> $GITHUB_ENV
          mkdir -p ${HOME}/.cache/ccache
          ccache -p || true
        shell: bash

      - name: Create Python venv
        run: |
          python3 -m venv .venv
          echo "${GITHUB_WORKSPACE}/.venv/bin" >> $GITHUB_PATH
          ${GITHUB_WORKSPACE}/.venv/bin/python -m pip install --upgrade pip setuptools wheel build pybind11 cython pybind11-stubgen==2.5.5 py-build-cmake==0.5.0 "cmake~=3.23.0"

      - name: Clone OpenVINO
        run: git clone --recurse-submodules --depth 1 https://github.com/openvinotoolkit/openvino.git -b releases/2026/0

      - name: Build and Install OpenVINO Core
        run: |
          df -h
          cd openvino
          mkdir -p build && cd build
          cmake .. \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX=${GITHUB_WORKSPACE}/ov_install \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
            -DCMAKE_C_FLAGS="-fdebug-prefix-map=${GITHUB_WORKSPACE}=." \
            -DCMAKE_CXX_FLAGS="-fdebug-prefix-map=${GITHUB_WORKSPACE}=." \
            -DENABLE_AVX2=OFF \
            -DENABLE_AVX512F=OFF \
            -DENABLE_PYTHON=ON \
            -DPYTHON_EXECUTABLE=${GITHUB_WORKSPACE}/.venv/bin/python \
            -DENABLE_WHEEL=ON
          
          ccache -s || true
          make -j$(nproc)
          make install
          ccache -s || true
          df -h
          python -m pip install wheels/*.whl
          # Verify installation and assign clean installation directory
          python -c "import openvino; print(f'OpenVINO version: {openvino.__version__}')"
          OV_DIR="${GITHUB_WORKSPACE}/ov_install/runtime/cmake"
          echo "DETECTED_OV_DIR=$OV_DIR" >> $GITHUB_ENV
          echo "Hardcoded OpenVINO_DIR to: $OV_DIR"

      - name: Save ccache (Mid-Job)
        uses: actions/cache/save@v4
        if: always()
        with:
          path: ~/.cache/ccache
          key: ccache-openvino-n6000-${{ github.run_id }}

      - name: Clone OpenVINO GenAI
        run: |
          df -h && df -i && free -m
          # Shallow clone for efficiency
          git clone --depth 1 https://github.com/openvinotoolkit/openvino.genai.git -b releases/2026/0
          cd openvino.genai
          # Serial update to avoid OOM/Spikes
          git submodule update --init --recursive --depth 1
          df -i && free -m

      - name: Build OpenVINO GenAI Wheels (Manual Build Process)
        run: |
          set -x
          df -h && free -m
          cd openvino.genai
          
          # Native Build with explicit verbose mode and strict parallelism
          export OpenVINO_DIR=$DETECTED_OV_DIR
          export CMAKE_C_COMPILER_LAUNCHER=ccache
          export CMAKE_CXX_COMPILER_LAUNCHER=ccache
          
          echo "Using OpenVINO_DIR: $OpenVINO_DIR"
          
          # 第一步：显式 CMake 配置
          mkdir -p build && cd build
          cmake .. \
            -DCMAKE_BUILD_TYPE=Release \
            -DOpenVINO_DIR=$DETECTED_OV_DIR \
            -DENABLE_PYTHON=ON \
            -DPYTHON_EXECUTABLE=${GITHUB_WORKSPACE}/.venv/bin/python \
            -DENABLE_WHEEL=ON \
            -DBUILD_TOKENIZERS=OFF \
            -DENABLE_SAMPLES=OFF \
            -DENABLE_TESTS=OFF
          
          # 第二步：安全双核并发 (锁住系统最大内存冗余)
          cmake --build . --config Release --parallel 2
          
          # 第三步：Python 包装
          cd ..
          export CMAKE_BUILD_PARALLEL_LEVEL=2
          python -u -m pip wheel . --no-deps --no-cache-dir --no-build-isolation -vv --wheel-dir ./dist
          df -h && free -m

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: n6000_openvino_wheels
          path: |
            openvino/build/wheels/*.whl
            openvino.genai/dist/*.whl
